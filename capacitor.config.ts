import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'avisos.comunitarios.v2',
  appName: 'avisos-comunitarios-v2',
  webDir: 'www'
};

export default config;
